export enum NativeType {
  String = 'string',
  Number = 'number',
  Boolean = 'boolean',
  Array = 'Array',
  Date = 'Date',
  File = 'File',
  Any = 'any',
  Void = 'void',
}

export enum AngularResponseType {
  Json = 'json',
  Text = 'text',
  Blob = 'blob',
  ArrayBuffer = 'arrayBuffer',
}

export enum AngularRequestType {
  Get = 'get',
  Post = 'post',
  Put = 'put',
  Delete = 'delete',
  Patch = 'patch',
}
